import { Pressable, Text, StyleSheet, Animated, Linking } from "react-native";
import Colors from "../constants/colors";

const ArticlePageButton = () => {
	return <div>ArticlePageButton</div>;
};

export default ArticlePageButton;
